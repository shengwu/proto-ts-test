//import { Item, List, TodoService } from "../generated/todo";

/*
const client = new TodoService.creste("https://my.grpc/server");
const list = client.getList({}, (err: any, list: List) => {
  console.log(list);
});
 */
